query User($id: Int!) {
  user(id: $id) {
    id
    username
    posts {
      id
      points
      voteStatus
      title
      text
      textSnippet
      creator {
        id
        username
      }
      createdAt
      updatedAt
    }
  }
}
